# Contracts

开工前定义：业务对象ID与版本、schema、输入输出、workflow transition、actor权限、Evidence定位、Finding、Decision、AuditEvent、错误与missing-context表达。

业务schema保存在scenario；跨场景证据充足后才提入kit。API与结构化输出共享契约源，生成代码不成为第二份真相。当前没有锁定OpenAPI/JSON Schema版本，首次实现需登记具体版本与验证工具。

来源、模型推断、确定性规则结果、人工决定分别记录；任何重要判断可反向定位原件/页码/段落或记录ID，并保留版本。
