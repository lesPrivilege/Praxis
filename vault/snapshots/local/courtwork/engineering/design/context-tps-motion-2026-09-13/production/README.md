# Chatspace Context / activity · production integration

2026-09-13 · Astra author. User accepted the visual candidate and authorized implementation, then clarified Chatspace as the primary surface, Attention by subtraction, other processes through a phrase carousel, and finally moved the ring to the right of model/effort next to Send. This records product application and author verification, not independent code or canonical visual acceptance.

## Result and owners

- Chatspace Context is a ring-only button between model/effort and Send (or Stop). It opens the retained request estimate, serialized character count and declared window. These remain separate facts; the ring has no capacity arc or percentage.
- Latest Run feedback is a persistent activity row in the message stream, with the waveform at the left and a short phrase. First-level TPS, missing-rate text and chevron are absent; the glyph opens the measurement card. Historical Run inspection and formal decision receipts retain their contracts.
- Working/Thinking phrases rotate every 3.5 seconds. Exact Run tool events select reading, writing, searching or general tools; assistant text deltas select composing; compaction/retry notices select those phases. Multiple open tools are tracked by call ID. Pending cancellation freezes decoration; only a host terminal fact says Cancelled. Waiting, failure, lost connection, hidden surfaces and reduced motion stop the clock. The app-level Always reduce preference is also observed.
- Attention consumes the same activity projection without measurement controls. Existing memory/runtime disclosure remains its own surface.
- Two native auto-popover cards reuse shared anchoring, solid raised material, focus and 180/120ms disclosure. Keyboard opening is instant, Escape returns focus, outside dismissal works. Snapshot data stays stable while reading; Refresh only rereads retained events. Session/Run changes close the card. Inspect run uses the existing target.
- No backend schema, provider token clock, dependency or authority changed. Missing decode TPS stays missing in details. Host timing is not provider TTFT. Specific prose such as Adding targeted tests is not promoted into an asserted progress fact.

Implementation: [activity projection and lifecycle](../../../../app/web/run-activity.mjs), [measurement disclosure](../../../../app/web/chat-measurements.mjs), [Chatspace](../../../../app/web/app.mjs), [Attention](../../../../app/web/attention-agent-view.mjs). Nearest implemented precedents are telemetry-view request disclosure, ui-controls anchorPopover/tooltips, composer controls and existing overlay tokens; PresenceView remains a motion reference only. A shared tooltip focus handler now defers reopening until native popover dismissal finishes, fixing a reproduced reentrant showPopover error.

## Evidence and limits

Author browser used an isolated temporary Runtime and loopback fake provider with synthetic data: [fixture](../../../../app/tests/fixtures/run-activity/serve.mjs). No personal data or paid provider. Start with `node app/tests/fixtures/run-activity/serve.mjs`; it prints its independent URL/data directory.

- Full suite: [937/937](evidence/full-tests-final.txt), 219.6 seconds. [First run](evidence/full-tests.txt) retained two semantic ledger/copy failures; the label and named-read ledger were corrected without broadening guard exceptions. A concurrent release writer independently registered the existing Work review refresh before this slice was committed.
- After the full run, the final ring relocation and app-level reduced-motion observer were added. [Final targeted checks: 19/19](evidence/final-targeted.txt) cover those activity/missing-data semantics; full-suite result above predates these final small changes. [Static checks](evidence/static-checks.txt), [contrast](evidence/contrast.txt) and [document links](evidence/doc-links.txt) record the final verification.
- Browser author checks: real synthetic send/stream/cancel, wait/answer/resume, failed request, Attention running/close/reopen/cancel; keyboard card open/Escape/focus, retained measurement details and Inspect run. Screenshots [streaming](evidence/03-streaming-response.png), [cancelled](evidence/04-cancelled.png), [Attention](evidence/05-attention-subtraction.png), [waiting](evidence/06-waiting-for-input.png), [failed](evidence/07-provider-failed.png).
- Desktop 1280 and 1440, narrow 390 and initial 476; light/dark card inspection and long response history. [390 light](evidence/08-context-mobile-light.png), [390 dark](evidence/09-context-mobile-dark.png), [1440 dark](evidence/10-measurements-desktop-dark.png) precede the final relocation. [Latest 390 placement](evidence/13-context-next-to-send-mobile.png) and [latest 1280 placement](evidence/14-context-next-to-send-desktop.png) supersede their left-ring placement. Screenshot 02 has a historical filename but shows completed measurement disclosure, not a running claim.
- User process references: [Running tools](evidence/11-user-running-tools.png), [Adding targeted tests](evidence/12-user-targeted-tests.png). First maps to existing tool lifecycle; second informs concise writing style but needs an explicit task-progress owner before literal dynamic projection. Five received Thinking words are included; no unreceived 185-word fixture is claimed.
- Reduced motion has deterministic lifecycle coverage; native OS reduced motion, forced-colors, reduced-transparency, real 200% zoom and screen-reader hardware were not exercised. Solid material needs no transparency fallback; forced-colors CSS and shared app reduction are present. This is not the complete accessibility matrix or independent acceptance. No push/deployment.

Product source hashes are retained in [source-sha256.txt](evidence/source-sha256.txt); integration began at `84574246ee4fd9302c9a607b2861a9d789ac90a1`, with concurrent main advancing to `44953e74bd5918db082d32f4e5ac60b400ce526c` during checks. Other writers’ edits are preserved.

## Ring refinement · user follow-up

Based on implemented `9a5694c`, the Context SVG box shrinks from 18 to 14px, stroke from 1.7 to 1.4 viewBox units, with 4px extra margin on each side using the existing spacing token. Desktop 32px / narrow 44px hit targets remain. This refines `context.capacity` beside the existing model and Send controls without changing measurement semantics. Author browser verified [390px](evidence/15-fine-ring-mobile.png) and [1280px](evidence/16-fine-ring-desktop.png); narrow document width equals viewport width, and computed ring/margin/hit size match 14/4/44px. Color and interaction lint pass. No new behavior tests or full-suite rerun for this geometry-only adjustment. Earlier hashes and captures describe the initial integration.

## Independent review and combined closure

[Closure record](closure.md) covers Luna’s independent findings and Astra’s repairs, acceptance of the separate Work review card on de55674, source-classification intake, and the user’s agent-time/smaller-action additions. Original author-only statements above describe the initial delivery; see the closure for final review and test scope.
