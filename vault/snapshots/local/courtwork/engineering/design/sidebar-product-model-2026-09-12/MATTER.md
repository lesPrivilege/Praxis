# Matter · Open record

2026-09-12 · Astra原创候选，Luna只读定位现有consumer。

Matter表示一项可持续推进、保留来源和版本的工作。图形采用开放记录框与两条短线；不采用Project文件夹、文件折角、插件或验收勾。沿现有Expert与三个产品glyph的24网格、2px圆端描边、currentColor语法，来源/hash进入同一sprite生成链。

最近已实现先例是`app/web/app.mjs`的`continueExistingSegment`：现有续接行已从`semanticPresentation("matter.object")`取得glyph。本轮只将registry的App表示从text改为单用途matter glyph，既有ID、版本、来源修订与点击handler继续由binding提供。Project/Workspace文件夹和producer插头保持原义，不增加Matter侧栏目的地。Pages仍用文字表示。

基线0744cd3；本轮是身份图形候选，不改变工作合同、状态、权限或正式接受。生成一致性、语义测试与局部视觉证据随整体验收记录；不以作者作图或测试通过宣称独立视觉接受。

用户[参考原图](references/05-matter-project-reference.png)与其hash已保存；图中Project文件夹与子任务只提供层级参考，不推导Matter产品入口。
