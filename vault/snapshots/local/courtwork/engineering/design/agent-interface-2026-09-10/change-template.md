# UI 局部变更记录

复制到实际工单/交付内填写，不另建平行事实库。简单修复可短写；无变化写无，不虚构计数。

```text
Task / scope:
Base SHA / branch / isolated checkout:
Writer / reviewer:

Owner fact + contract:
Semantic / projection / control / placement:
Affected UX rule IDs / persistent text purpose / disclosure level:
Action result / feedback / recovery / draft and scope identity:
Nearest precedent: repository path + symbol + evidence + fixed SHA
Evidence type: implemented precedent / accepted baseline / candidate / external reference
Governance status: canonical / reference / unverified / deferred (with evidence and scope)
Kept relationships:
Intentional changes:

New terms / roles / tokens / primitives / dependencies: none or explicit list
Reuse / variant / grammar gap decision:
Skin / review / deterministic semantic color impact:
Exceptions: rule, reason, scope, owner, removal or revisit condition

Fixture and setup command (synthetic; no personal data):
Affected scene + nearest adjacent scene + full composition:
Viewport / scheme / keyboard / failure / zoom / fallback coverage:
Checks: exact command, exit/result, output/evidence path; not-run and reason
Visual change: before/after at fixed SHA; candidate baseline paths
Author checks:
Independent review: reviewer, exact SHA, scope, findings/result
Remaining work / accepted-baseline decision:
```

标记某截图为golden须有接受记录；仅有文件存在不够。改了先例符号/路径就同步[先例索引](precedents.md)。新语义仍回写原合同/词表，不在此模板另起名称。当前手动填写，不冒充自动PR报告。
