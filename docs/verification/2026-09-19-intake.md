# 初始入账与增量验收 · 2026-09-19

范围：截至工作系统r2的已完成批次；后续r3单独增量验收。本记录保留历史数值。

编订与最终一致性验收：Astra。探索、追溯、摘要与材料提炼：Luna 集群。

## 覆盖

| 项目 | 结果 |
|---|---|
| Chat | 3个对话；最新完整版本21轮、42条消息；全部映射到结构化提炼 |
| 工作系统增量 | r1的5轮保留；r2新增4轮/8消息；原5轮消息内容不变 |
| 外部溯源 | 29个显式URL均登记；115个引用占位均有映射 |
| 来源卡 | 142条来源记录，对应139个不同URL；每条有本地摘要、状态、限制及重访条件 |
| 本地项目 | Courtwork 219文件（48深读、171 indexed）；career-kit 10文件深读 |
| Downloads | 109源文件、10 ZIP、136解包文件；21个源HTML作静态读取 |
| 完整性 | 484份本地资料快照 + 5份Chat捕获 = 489；472个不同内容hash |
| 实现与远端 | 治理/知识/契约基线；无运行demo。origin已连接用户的公开Praxis仓库，未提交或推送 |

## 本次验证

依次运行：

```sh
python3 scripts/render_source_cards.py
python3 scripts/build_registry.py
python3 scripts/build_snapshot_manifest.py
python3 scripts/validate_repository.py
```

结果：受治理目录入口、本地Markdown链接、JSON解析、来源卡存在性、42条消息提炼映射、115条引用映射、29个明确URL覆盖、索引新鲜度、489份快照hash均通过。源复制件同时与intake登记的原件hash核对。

快照中的原有文档链接不改写，不把第三方目录强制重排；其可消费摘要与本地入口由本Kit维护。检查范围是本轮受治理目录，不含并发出现的其他目录或配置。

## 保留的边界

- 隐藏引用URL没有因本轮补查而恢复。明确链接的邻文映射与另找官方来源的supplemental映射分别登记。
- 来源状态是对特定主张的核查结果；partial/unverified/unavailable仍保留，不能解释为全量背书。按catalog当前值消费。
- Courtwork的171件indexed材料已可本地追溯，未声称逐件深读。Downloads核心材料深读，其余包内资源按索引/依赖保留。
- Downloads静态扫描有13个远端引用族、11个缺失本地依赖。未执行HTML或包内脚本，不能声称所有原型离线可运行或视觉QA通过。
- 本地研究快照不自动取得公开发布资格。连接公开origin不等于上传原始资料；后续发布范围需要明确选择。
- 未安装研究清单中的依赖、启用自动化、修改来源项目或迁移全局工作目录。

## 后续增量

从 [入账规范](../governance/intake.md) 开始；新证据说明支持/扩充/挑战哪个旧记录。保留旧快照，更新catalog、提炼与ADR，再重建索引并验证。命名与架构均允许按 [演进机制](../governance/evolution.md) 修订。
