# Praxis 初次发布验收 · 2026-09-19

## 范围

覆盖 Enterprise、Reporting 与 Work System r1–r4。当前用户确认不存在企业数据；组织 Source Vault、客户项目和 Kit 的隔离是未来架构契约，不表示本次持有客户资料。

- 3 份对话，32 轮、64 条消息，均有逐消息消费映射。
- 29 个明确外链、137 个引用占位；170 条来源记录对应 167 个不同 URL，逐源保留摘要、核验状态、限制和重访条件。
- 491 个归档/材料快照均登记 hash。包含 Courtwork、career-kit 和 Downloads 的选定材料与必要依赖。
- 原 Chat 仅供备查；结构化提炼、契约与分层索引作为默认消费入口。

## Astra 收敛

[结构复核](luna-structure-review-2026-09-19.md) 的两项 P1 已处理：Vault 默认入口补齐 r3/r4，根 README 改为指向本次验收。P2 同步处理：根 README 作为第一性原理权威文本，Agent 指引保留 Goal 摘要与链接；架构理由补充 r4 澄清入口；外源目录说明 r4 无新 citation；验收索引保留历史批次。

企业数据分区、薄 Skill、可替换 runtime 和基于证据与验收的抗折旧理由进入当前规范。Courtwork Expert/composer、权限执行、bootstrap 与训练路线仍是后续消费候选。Expert 的产品语义不能覆盖宿主指令层级。

## 验证与限制

运行全套本地索引生成与一致性验证，并对暂存树导出后的独立副本再次验证。检查覆盖消息身份、来源与引用映射、链接、JSON、来源卡及快照 hash。

来源中的 partial/unverified/unavailable 保留原状态；补充溯源不能冒充恢复了原 Chat 隐藏引用。Downloads 尚有 13 类远程引用和 11 处缺失本地依赖，静态材料可阅读，不保证每个 HTML 原型均能离线运行。没有运行来源脚本或构建 runtime。

[Luna 发布检查](luna-publication-review-2026-09-19.md) 与 [实际执行回执](run-receipt.json) 独立保留。token、费用与 quota 没有可核验回执，未估造数字。

历史批次见 [截至 r2 的验收](2026-09-19-intake.md)，其旧数字保持不变。后续增量必须生成新的有范围验收，不能把本次通过视为永久保证。

## 发布复核裁决

Luna 静态检查未发现实际凭据、联系方式或客户原始数据。其按本地目录身份排除 Chat、career-kit、Courtwork、Downloads 快照及追溯标识的建议不采纳：用户明确要求这些已筛选材料在 Kit 内独立保存、溯源，并授权收敛后 push；本轮也明确不存在企业数据。原始路径和消息 ID 保留为来源定位，不能当作运行依赖；公开摘要不等于修改原件。未发现具体权利冲突，不额外设立推测性审批。临时文件、缓存和并发项目按 .gitignore 排除。
