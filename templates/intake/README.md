# Intake record templates

使用 [材料模板](material-record.json) 建立新入账批次，使用 [来源卡模板](source-card.md) 撰写可阅读摘要。既有catalog可保留特有字段，但必须表达相同的身份、证据状态、摘要、去向和缺口；不为空字段编造值。

`null` 表示未知或尚未处理；不要用空串伪装已验证。稳定ID一经使用不因标题变化重编。示例模板不能计入材料覆盖统计。
